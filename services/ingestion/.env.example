PORT=8080
WORKER_COUNT=10
MAX_FILE_SIZE_MB=15
JWT_SECRET=changeme

# Storage backend: "s3" (default) or "local" (for local development)
STORAGE_BACKEND=local
LOCAL_STORAGE_DIR=local-storage

# Required when STORAGE_BACKEND=s3
AWS_REGION=ap-southeast-2
S3_BUCKET=your-bucket-name
